# 02 — `server/ai/`

Read `00-context.md` first. Commit 01 must be done.

## Goal

Gather everything that talks to a language model or a translation API into
`server/ai/`: the AI tasks, the persistent queue, the providers, the chat, and
the AI endpoints currently inside `server/routes/articles.ts`. **No behaviour
change.**

## Moves (`git mv`, tests alongside)

| From | To |
|---|---|
| `server/fetcher/ai.ts` (+ `.test.ts`) | `server/ai/tasks.ts` |
| `server/fetcher/ai-queue.ts` (+ `.test.ts`) | `server/ai/queue.ts` |
| `server/providers/llm/*` | `server/ai/providers/llm/*` |
| `server/providers/translate/*` | `server/ai/providers/translate/*` |
| `server/chat/*` | `server/ai/chat/*` |
| `server/chatRoutes.ts` (+ `.test.ts`) | `server/ai/chat/routes.ts` |
| `server/chat-api.test.ts` | `server/ai/chat/api.test.ts` |

`server/providers/` and `server/chat/` end up empty; remove the directories.

Fix relative imports inside the moved files. Notable ones:

- `ai/tasks.ts` (was `fetcher/ai.ts`): `../providers/llm/index.js` →
  `./providers/llm/index.js`; `../providers/translate/*.js` → `./providers/translate/*.js`;
  `../db.js` stays; `../../shared/*` stays.
- `ai/queue.ts` (was `fetcher/ai-queue.ts`): `./ai.js` → `./tasks.js`;
  `./util.js` → `../fetcher/util.js`; `../db/*.js` stays.
- `ai/chat/title-generator.ts`: `../providers/llm/index.js` → `../providers/llm/index.js`
  (same depth, now resolves inside `ai/`). Check every file under `ai/chat/`.
- `ai/chat/routes.ts` (was `server/chatRoutes.ts`, one level deeper now):
  `./chat/*.js` → `./*.js`; `./auth.js` → `../../auth.js`; `./lib/*.js` →
  `../../lib/*.js`; `./db.js` → `../../db.js`; `../shared/*` → `../../../shared/*`.
- `ai/chat/api.test.ts` and `ai/chat/routes.test.ts`: same depth change; their
  `vi.mock('./chat/adapter.js')` becomes `vi.mock('./adapter.js')`, their
  `vi.mock('./fetcher.js')` becomes `vi.mock('../../fetcher.js')`, and the
  `./__tests__/helpers/*` imports become `../../__tests__/helpers/*`.

## Extract the AI endpoints out of `server/routes/articles.ts`

Move, verbatim, into `server/ai/routes.ts` as `aiArticleRoutes(api)`:

- `getTranslateTargetLang()` (line 58). If anything left in `routes/articles.ts`
  still uses it, do not keep a copy: move it and import it back from
  `../ai/index.js`.
- The block from `// --- Known error codes that the frontend can i18n-translate ---`
  (line 129) through `formatUsage` (ends line 233): `KNOWN_ERROR_CODES`,
  `extractKnownErrorCode`, `AiHandlerConfig`, `createAiHandler`, `formatUsage`.
- The two routes `POST /api/articles/:id/summarize` (lines 540–560) and
  `POST /api/articles/:id/translate` (lines 561–594), including the
  `translateArticleTitle` call in the translate `applyResult`.

Their imports (`summarizeArticle`, `translateArticle`, `streamSummarizeArticle`,
`streamTranslateArticle`, `AiTextResult`, `translateArticleTitle`, `startSSE`,
`updateArticleContent`, `updateScore`, `getArticleById`, `requireJson`,
`NumericIdParams`, `parseOrBadRequest`, …) move with them. Remove from
`routes/articles.ts` every import that only the moved code used.

Register `aiArticleRoutes` in `server/routes/index.ts` right after
`articleRoutes`, so route registration order and URL prefixes are unchanged.

`server/routes/articles.test.ts` keeps testing the two endpoints through the
app; it must pass unmodified except for the `vi.mock('../fetcher/ai-queue.js')`
path (see below).

## New files

### `server/ai/index.ts`

- from `tasks.ts`: `detectLanguage`, `_clearDetectLanguageCache`,
  `evaluateArticleRelevance`, `translateSnippet`, `translateTitle`,
  `summarizeArticle`, `streamSummarizeArticle`, `translateArticle`,
  `streamTranslateArticle`, `type AiTextResult`, `type AiBillingMode`.
- from `queue.ts`: `enqueueAutoTranslate`, `enqueueAutoSummarize`,
  `enqueueAiFilter`, `isAutoTranslateEnabled`, `isAutoSummarizeEnabled`,
  `resumePendingAiTasks`, `translateArticleTitle`, `getAutoTranslateScope`,
  `_resetAiQueueForTests`.
- from `providers/llm/index.ts`: `getProvider`; from `providers/llm/provider.ts`:
  `type LLMProvider`.
- from `providers/translate/*`: `getMonthlyUsage`, `getDeeplMonthlyUsage`
  (used by settings routes).
- from `chat/routes.ts`: `registerChatApi`.
- from `routes.ts`: `aiArticleRoutes`, `getTranslateTargetLang`.

## Importers to update

- `server/fetcher.ts` (façade): the `./fetcher/ai.js` export lines → `./ai/index.js`.
- `server/ingest/fetch-content.ts`: `../fetcher/ai.js` → `../ai/index.js`.
- `server/ingest/run.ts`, `server/ingest/steps/ai-queue.ts`, `ai-filter.ts`:
  `../fetcher/ai-queue.js` → `../ai/index.js` (steps: `../../ai/index.js`).
- `server/ingest/steps/ai-queue.test.ts`, `ai-filter.test.ts`,
  `server/routes/clip-articles.test.ts`, `server/routes/articles.test.ts`:
  `vi.mock('…/fetcher/ai-queue.js')` → the `ai/index.js` path at the same depth,
  same factory.
- `server/routes/articles.ts`: `translateArticleTitle` import goes away with the
  translate route; whatever AI import remains comes from `../ai/index.js`.
- `server/routes/comments.ts`: `translateSnippet` from `../ai/index.js`;
  `server/routes/comments.test.ts`: `vi.mock('../fetcher/ai.js')` → `vi.mock('../ai/index.js')`.
- `server/rss-bridge.ts`: `./providers/llm/index.js` → `./ai/index.js`
  (`getProvider`, `type LLMProvider`); `server/rss-bridge.test.ts`: mock path likewise.
- `server/routes/settings.ts`: the two `../providers/translate/*.js` imports →
  `../ai/index.js`.
- `server/index.ts`: `./chatRoutes.js` → `./ai/index.js`.
- `server/__tests__/helpers/buildApp.ts`: `../../chatRoutes.js` → `../../ai/index.js`.
- `server/ai/tasks.test.ts` (was `fetcher/ai.test.ts`): `vi.mock('../providers/llm/index.js')`
  → `vi.mock('./providers/llm/index.js')`; `ai/chat/title-generator.test.ts` likewise.

Finish with `grep -rn "fetcher/ai\|providers/llm\|providers/translate\|chatRoutes\|from '\./chat/\|from '\.\./chat/" server scripts --include=*.ts`
and make sure every hit is inside `server/ai/`.

## Acceptance

- `ls server/fetcher/ai.ts server/fetcher/ai-queue.ts server/chatRoutes.ts server/providers server/chat` → every path missing.
- `grep -c "createAiHandler\|summarize'\|/translate'" server/routes/articles.ts` → 0.
- `server/routes/articles.ts` is under 650 lines.
- Outside `server/ai/`, the only `ai/` import path is `ai/index.js`.
- Typecheck, lint, server tests green; count unchanged (1,924).

Commit: `refactor(ai): gather AI tasks, queue, providers, chat and AI endpoints under server/ai`.
